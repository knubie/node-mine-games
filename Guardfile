options = {
  :slim => { :pretty => true },
  :input_root => File.expand_path('../client/src', __FILE__),
  :output_root => File.expand_path('../client', __FILE__)
}
guard 'slim', options do
  watch(%r{.+\.slim})
end
